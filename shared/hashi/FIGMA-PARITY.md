# Figma Library Parity Tracker

Cross-session state for the hashi code <-> Figma library parity effort. One row per component. Updated in every parity PR.

Component files live in per-component folders directly under their root (`<root>/<name>/<name>.tsx`); tracker rows below key by `<root>/<name>` — the parity test collapses the folder segment, so row keys are unaffected by the layout.

- Canonical library: **Hashi Design System**, file key `IZhfgAOIPjDObsCtpFKhRY`. All `.figma.tsx` URLs point here.
- `verdict`: `existing` (built + connected before this effort) / `build` / `map-to-existing <page>` / `skip (reason)`
- `status`: `pending` / `built` (in Figma, awaiting connect) / `connected` (`.figma.tsx` authored, parse green) / `merged` (PR merged to staging)
- Plan: `~/.claude/plans/ok-let-s-make-a-snappy-allen.md` (Zuri's machine). Build batches: 1 brand leaves, 2 simple visuals, 3 switchers+identity, 4 nav+onboarding, 5 timeline+overlays+map-to-existing.

## Page presentation template (2026-07-06)

Every component page (25 legacy + all parity builds) uses the shared spec-sheet template: 640px Spec Header card (tier eyebrow, name, summary, mono import chip, Code Connect meta dot, divider, description) + a surface/muted Showcase Stage the component set sits on, with a VARIANTS/PREVIEW caption and axis labels. Full build spec: `shared/hashi/docs/figma-page-template.md`. Apply it to every new page in batches 3-5. Icons page `41:2` and Table page `149:2` intentionally not templated (icon grid / other effort).

## Figma page inventory (audited 2026-07-06)

Foundations: Cover `2:7`, Getting Started `17:2`, Colors `17:4`, Typography `17:5`, Spacing & Radius `17:6`, Shadows `17:7`.
Component pages: Icons `41:2`, Button `17:9`, Badge `17:10`, IconButton `17:11`, Input `17:12`, Label `17:13`, Checkbox `17:14`, Switch `17:15`, Toggle `17:16`, Separator `17:17`, Skeleton `17:18`, Avatar `17:19`, ColorDot `17:20`, Tooltip `77:2`, Card `77:3`, Tabs `77:4`, Select `77:5`, DropdownMenu `77:6`, Breadcrumb `77:7`, Empty `77:8`, StatusDot `77:9`, WorkspaceStatusDot `77:10`, WorkspaceAvatar `77:11`, WorkspaceTypeVersionBadge `77:12`, MemberAvatarStack `77:13`, Table `149:2` (no code counterpart yet; belongs to the table-system effort).
Screens: Org Overview `97:3`.

Audit finding 2026-07-06: the library contains exactly the 25 already-connected components; none of the 44 gap components exist in Figma.

## Existing (connected before this effort)

| component | verdict | figma node | status | notes |
| --- | --- | --- | --- | --- |
| primitives/avatar | existing | 75-2 | merged | |
| primitives/badge | existing | 61-11 | merged | 144-variant set, emphasis/radius/size axes (#211) |
| primitives/breadcrumb | existing | 85-5 | merged | |
| primitives/button | existing | 46-2 | merged | |
| primitives/card | existing | 80-5 | merged | |
| primitives/checkbox | existing | 68-11 | merged | |
| primitives/color-dot | existing | 76-11 | merged | |
| primitives/dropdown-menu | existing | 84-5 | merged | compound-connect pattern reference |
| primitives/empty | existing | 86-5 | merged | |
| primitives/icon-button | existing | 65-26 | merged | |
| primitives/input | existing | 66-11 | merged | |
| primitives/label | existing | 67-9 | merged | |
| primitives/select | existing | 82-29, 83-8 | merged | two connects |
| primitives/separator | existing | 72-7 | merged | |
| primitives/skeleton | existing | 73-5 | merged | |
| primitives/switch | existing | 69-13 | merged | |
| primitives/tabs | existing | 81-22 | merged | |
| primitives/toggle | existing | 71-20 | merged | |
| primitives/tooltip | existing | 79-5 | merged | |
| components/MemberAvatarStack | existing | 91-5 | merged | |
| components/StatusDot | existing | 87-23 | merged | |
| components/WorkspaceAvatar | existing | 89-13 | merged | |
| components/WorkspaceStatusDot | existing | 88-8 | merged | |
| components/WorkspaceTypeVersionBadge | existing | 90-15 | merged | |
| icons (51 connects) | existing | pages 41/54/55/56/145/378/416 | merged | batch file `src/icons.figma.tsx` |

## Gaps (classified 2026-07-06; verdicts pending Zuri sign-off via this PR)

Verdict summary: **34 build, 6 skip, 4 map-to-existing** (of 44). Consolidations proposed: Flow/Project/WorkspaceSwitcherPill = variants of one SwitcherPill set; Org/Project/WorkspaceIdentityBand = variants of one PageIdentityHeader set.

| component | verdict | figma node | status | notes |
| --- | --- | --- | --- | --- |
| primitives/alert | build | 380-1666 | connected | BUILT 2026-07-09 (page `Alert`, PR #232): 24-variant set variant(6) x emphasis(light/bold) x shape(inline/banner) on the standard spec-header template; Title/Description text props, Show icon/Show description/Show action/Dismissible booleans, Action instance-swap defaulting to Button outline sm. Tinted light surfaces bind alpha companion variables (tint/x-15 fills, tint/x-30 + tint/foreground-20 strokes) because Figma instances drop paint-level opacity on variable-bound fills; Badge has the same latent bug (follow-up). Added Icon/info + Icon/circle-alert to the Icons page. Connect maps variant/emphasis/shape enums + Show description/Show action/Dismissible booleans. `Show icon` is design-only: the code auto-renders the variant icon whenever the `icon` prop is omitted, and Code Connect cannot emit a `null` literal, so an icon-less design needs `icon={null}` added by hand. Dismiss pins to the top-right corner; the variants carry a constant 40px right padding (code applies pe-10 only when dismissible, a boolean Figma padding cannot follow). Action buttons get alert-scoped hover tints mixed from the alert foreground (hover states are not modeled in Figma). |
| primitives/alert-dialog | build | 360-1237 | connected | BUILT 2026-07-07 (page `AlertDialog`): size default/sm x hasMedia, 4 static content panels (no portal/backdrop). default = 512px left-aligned, right footer; sm = 320px centered, two-column footer grid. Media box instances Icon/triangle-alert on a destructive/10 tile (WithMedia story treatment). Cancel/action are library Button instances. Connect: figma.enum(size) + figma.boolean(hasMedia) mapping to an AlertDialogMedia JSX slot. |
| primitives/collapsible | skip | - | skipped | Signed off (batch 1 chat). The source file applies zero CSS classes, it is a transparent pass-through to Base UI with only data-slot attributes; every visual treatment in the stories is applied by the consumer via inline className strings on trigger and content elements, so there is no visual identity to capture in Figma. |
| primitives/dialog | build | 365-33 | connected | BUILT 2026-07-07 (page `Dialog`): showCloseButton true/false, 512px static content panels (Re-run pipeline story copy; header + description + Button footer). Corner close = Icon/x instance at 16px, opacity 0.7, fill-recolored. Connect: figma.boolean(showCloseButton). |
| primitives/progress | build | 250-17 | connected | matrix: state: determinate/indeterminate, 2 total · deps: - · connect: figma.enum("state", { determinate: "determinate", indeterminate: "indeterminate" }) · The track is h-1.5 rounded-full; the indicator fills left-to-right as a percentage. ProgressLabel and ProgressValue are text slots above the track, not structural variants, represent them as optional layer content. Indeterminate omits the indicator fill entirely. |
| primitives/sheet | build | 366-1266 | connected | BUILT 2026-07-07 (page `Sheet`): side right/left/top/bottom x showCloseButton, 8 static panels. right/left = 384x420 full-height with vertical border on the viewport-facing edge; top/bottom = 640px wide, content-hugging, horizontal border. Sides-story copy per variant. Connect: figma.enum(side) + figma.boolean(showCloseButton). |
| primitives/slider | build | 251-13 | connected | matrix: state: enabled/disabled, 2 total · deps: - · connect: figma.boolean("disabled") · Track is h-1.5 rounded-full; indicator fills from left edge to thumb position; thumb is size-4 rounded-full with border-2 border-primary bg-white. Value/position is represented in Figma as a thumb placement annotation, not a separate variant. Disabled renders at opacity-50. |
| primitives/sonner | skip | - | skipped | Signed off (batch 1 chat). The Toaster component is a runtime portal container (wraps the third-party Sonner library) that renders nothing until toast() is imperatively called; there is no static visual surface to capture, confirmed skip per the approved plan. |
| primitives/textarea | build | 252-13 | connected | matrix: state: empty/filled/disabled/invalid, 4 total · deps: Label · connect: figma.enum("state", { empty: "empty", filled: "filled", disabled: "disabled", invalid: "invalid" }) · Shares the same border/focus-ring/bg token pattern as Input (already in Figma); build as a sibling. Field uses field-sizing-content (auto-grows) and rounded-lg border. The rows prop is a layout annotation, not a Figma variant, represent at the default 4-row height. |
| primitives/toggle-group | build | 253-488 | connected | matrix: variant: default/outline/pill; size: sm/default/lg, 9 total · deps: Toggle · connect: figma.enum("variant", { default: "default", outline: "outline", pill: "pill" }), figma.enum("size", { sm: "sm", default: "default", lg: "lg" }), figma.enum("spacing", { joined: "0", separated: "1" }), figma.enum("content", { text: "text", iconText: "icon+text", iconOnly: "icon-only" }) · spacing=0 (joined): items share borders, outer corners only on first/last, no gap. spacing=1 (separated): items have individual borders with gap-1, used primarily with pill variant. content drives whether items show text, icon+text, or icon-only, represent as instance swap on item content. States (selected, unselected, one-disabled, all-disabled) are item-level pressed/disabled states, not top-level group variants. |
| components/BrandMarkTile | build | 244-11 | connected | matrix: brand: zenml / kitaru; total 2 variants · deps: ZenmlMarkIcon, KitaruMarkIcon (both built as separate components on their own pages) · connect: figma.enum('brand', { zenml: ..., kitaru: ... }) · ZenmlMarkIcon and KitaruMarkIcon are instanced inside this tile; build those pages first so BrandMarkTile can reference them. |
| components/GrainGradientBg | skip | - | skipped | Signed off (batch 1 chat). Pure WebGL shader via @paper-design/shaders-react; stories explicitly note 'the WebGL canvas may render blank under headless Chromium, that is expected.' There is no static visual surface to represent in Figma, confirming the approved plan's skip flag. |
| components/KitaruLogo | build | 243-14 | connected | matrix: No structural prop variants, className only; 1 frame · deps: - · connect: No enum/boolean props; Code Connect maps the single SVG frame. · Ring pinned to brand/kitaru-orange (ALWAYS orange, never theme-driven; no ZenML-themed Kitaru logo exists). Animated ring is static in Figma. KitaruMarkIcon is the ring element extracted into its own component, keep as separate pages. |
| components/KitaruMarkIcon | build | 241-13 | connected | matrix: No structural prop variants, className only; 1 frame · deps: - · connect: No enum/boolean props; Code Connect maps the single SVG frame. · Canonical size inside BrandMarkTile is size-7 (28px); document that in the Figma frame description. Fill pinned to brand/kitaru-orange (ALWAYS orange, never theme-driven). Static ring in Figma, animation is runtime CSS. |
| components/OrgMark | map-to-existing | 75-2 | connected | CONNECTED 2026-07-07: variant-restricted connect {shape: square} on the existing Avatar set; size sm/xl mapped to the Avatar size variants. No new Figma build. |
| components/ZenmlLogo | build | 242-17 | connected | matrix: No structural prop variants, className only; 1 frame · deps: - · connect: No enum/boolean props; Code Connect maps the single SVG frame. · Entirely currentColor, Figma frame should use a single fill style so light/dark adaptation is handled by theme without separate variants. |
| components/ZenmlMarkIcon | build | 241-7 | connected | matrix: No structural prop variants, className only; 1 frame · deps: - · connect: No enum/boolean props; Code Connect maps the single SVG frame. · Canonical size inside BrandMarkTile is size-7 (28px). Static SVG shape in Figma, no animation to represent. |
| components/SwitcherPill | build | 282-506 | connected | BUILT 2026-07-06 (page `280:4`): pill set 282-506 (labelVariant linked/static x labelContent text/rich, 4 frames), SwitcherRow set 282-507 (context flow/project/workspace x state active/inactive, 6 frames per Zuri's one-set-incl-context-variants ask), SwitcherEmpty 282-504. Deviation vs audit: the code has NO divider and NO outer border on the pill (the "divider" only exists in a comment); linked and static are visually identical at rest, kept as API-documenting variants. New text styles: text/sm-semibold, text/xs-italic. |
| components/SwitcherDropdownMenu | build | 284-652 | connected | BUILT 2026-07-06 (page `283:2`): 5 sparse variants (footer none/action/counter x headerExtras none/badge x content populated/empty) composed from SwitcherRow + SwitcherEmpty instances inside the DropdownMenuContent popover chrome (w-320, shadow/md). Search field hand-built (Input instance has no leading-icon slot); fills cloned from the Input component. |
| components/FlowSwitcherPill | build | 282-507 | connected | BUILT 2026-07-06: context=flow variants of the shared SwitcherRow set (StatusDot + name + "N ver · M exec" counter). Own .figma.tsx with variant-restricted connect {context: flow}; no separate page. |
| components/ProjectSwitcherPill | build | 282-507 | connected | BUILT 2026-07-06: context=project variants of the shared SwitcherRow set (name-only row). Variant-restricted connect {context: project}. |
| components/WorkspaceSwitcherPill | build | 282-507 | connected | BUILT 2026-07-06: context=workspace variants of the shared SwitcherRow set (name + mono version Badge). Variant-restricted connect {context: workspace}. Grouped ZenML/Kitaru sections + back-to-organization footer are shown on the SwitcherDropdownMenu page, not as row variants. |
| components/Breadcrumbs | map-to-existing | 85-5 | connected | CONNECTED 2026-07-07: points at the existing Breadcrumb (primitive) node; Crumb 1/Crumb 2/Current page strings mapped into the items array (isCurrent on the last). No new Figma build. |
| components/TabsBar | build | 299-2 | connected | BUILT 2026-07-06 (page `296:2`): 6 sparse variants, axes surface (page/panel) x badge x actions (none/single/multi), 760px bars. Active trigger = primary text + 2px primary underline overlapping the bottom border; inactive labels bound text/muted-foreground per the existing Tabs line-variant precedent (code is text-foreground/75 in light mode); badge chip mono/2xs on surface/muted, min-width 18. Action slots instance the Button set with lucide icon swaps (play/refresh-ccw/arrow-left-right) + label overrides. Connect maps badge via a lastTab object-value figma.boolean; multi actions wrapped in span.contents (no fragments in enum values). |
| components/AppNavbar | build | 301-915 | connected | BUILT 2026-07-06 (page `301:2`): 4 sparse frames, axes leading (empty/brand/breadcrumbs) x trailing (empty/actions), 960px bars. Composes NavbarBrandSlot, IconButton (back/bell, icon swaps), Button (New run + plus), and Avatar sm (AK) instances; brand frames drop paddingLeft to 0 since the slot eats the px-5 gutter. Breadcrumb scope is text spans per the stories (switcher-pill breadcrumbs are app-navbar composition, not this shell). DEVIATION vs audit connect: both axes map via figma.enum to JSX slot values, not figma.instance. |
| components/NavbarBrandSlot | build | 244-437 | connected | matrix: brand: zenml / kitaru; render: div(static) / link(navigable), total 4 frames · deps: BrandMarkTile (ZenML/Kitaru logo mark + wordmark, no existing Figma page) · connect: brand: figma.enum('zenml', 'kitaru'); isLink: figma.boolean |
| components/IdentityBand | skip | - | skipped | Signed off (batch 1 chat). Pure structural layout wrapper (bg-secondary + border-b + fixed padding) with no variant props, no visual identity of its own, and zero enumerated story variants, the entire visual surface is caller-supplied children. |
| components/PageIdentityHeader | build | 288-1091 | connected | BUILT 2026-07-06 (page `285:3`): ONE 16-frame set, axes context (generic/org/project/workspace) x config (sparse) instead of the planned booleans - one enum axis scales across all four contexts. generic = default/with-meta/with-actions/no-pretitle (4). 1200px band frames, 1024 centered container, flat bg-card (code has NO border-b). New text style text/lg-semibold. Parser gotcha: importing @zenml/hashi/primitives/avatar in a .figma.tsx fails with "reading 'split'"; use the relative ../primitives/avatar import. |
| components/OrgIdentityBand | build | 288-1091 | connected | BUILT 2026-07-06: context=org variant of the PageIdentityHeader set (1 canonical frame: primary-tinted imageless org mark, ORGANIZATION eyebrow, 3-stat cluster). Variant-restricted connect {context: org}. |
| components/ProjectIdentityBand | build | 288-1091 | connected | BUILT 2026-07-06: context=project variants of the PageIdentityHeader set, config=members-single/few/overflow (3 frames, MemberAvatarStack visibility overrides). DEVIATION: the audit's copied axis was dropped - the copied state renders inside the CLOSED overflow menu, so idle/confirmed frames would be pixel-identical (verify-claims-against-CSS). Variant-restricted connect {context: project}. |
| components/WorkspaceIdentityBand | build | 288-1091 | connected | BUILT 2026-07-06: context=workspace variants of the PageIdentityHeader set, 8 story frames (zenml-running/kitaru/degraded/idle/upgrade-available/no-external-urls/many-members/command-copied). Login-command chip is a named "login-command" sub-frame. Deviations: URL/API Button instances have no leading copy/external-link icons (Button set has no icon slot); no-external-urls chip shows "kitaru login http://localhost:8237" instead of the literal "kitaru login undefined" the story renders (looks like a code quirk, flagging rather than replicating). Variant-restricted connect {context: workspace}. |
| components/WorkspaceProjectCard | build | 292-930 | connected | BUILT 2026-07-06 (page `290:2`): 8 variants (gradient x statistics), 256px cards, 2-col wrap set. Banner gradients are to-br GRADIENT_LINEAR with variable-bound stops: zen-green=brand/primary .85/.55/.25, zen-slate=text/muted-foreground .55/.30/.10, zen-sand=chart/2 .70/.45/.15, kitaru-amber=chart/1 .85/.55/.25. Figma gotcha: binding a variable to a gradient stop resets its alpha to 1 - rewrite stop alphas in a second fills assignment. New text styles: mono/base-semibold (JetBrains Mono ships no SemiBold, mapped to Bold), text/xs-semibold. Resting card flat per no-static-shadow rule. |
| components/OnboardingChecklist | build | 309-945, 312-1049 | connected | BUILT 2026-07-06 (page `307:2`): OnboardingChecklistItem set 309-945 (state x body, 8 variants, 392px rows) + OnboardingChecklist card set 312-1049 (progress fresh/mid/complete, 440px cards composing item instances with index/title/paragraph overrides). Master current+body carries the device paragraph + uv-pip code block; the fresh card hides the code block via a visible=false override. DEVIATION: fresh = 20% (1 of 5, implicit workspace step) and mid = 60% (3 of 5) per getOnboardingSetup story fixtures, not the audit's 0%/40%. Item connect maps state to the isCompleted/isActive/hasDownstreamStep boolean triple; card connect maps progress to itemsDone. |
| components/OnboardingProgress | build | 305-929 | connected | BUILT 2026-07-06 (page `305:2`): one set, 12 variants (bar value 0/50/100 with size=-, ring size sm/md/lg x value 0/50/100). Bars drawn as 288x4 surface/muted track + brand/primary fill (the Figma Progress set is the labeled determinate/indeterminate shape, wrong anatomy to instance). Rings = ellipse track + cubic-bezier vector arc with round caps (vectorPaths rejects SVG A commands), strokeWidth size/12 per the RingSizes story. decorative not represented (AT-only). Two variant-restricted connects (Bar type=bar, Ring type=ring); strokeWidth dropped from the ring example - computed expressions like {size / 12} are a Code Connect ParserError. |
| components/OnboardingWidget | build | 315-1270, 315-1271, 315-1262 | connected | BUILT 2026-07-06 (page `314:2`): OnboardingWidgetStepRow set 315-1270 (state done/current/skipped/upcoming, 264px rows), OnboardingWidgetCard set 315-1271 (progress x actions, 320px cards with shadow-lg composing step-row instances), OnboardingWidgetPill 315-1262 (single component instancing the sm/50% progress ring - closest enumerated value to the 3-of-5 fixture). DEVIATION vs audit connect: no single view enum; three separate connects, Card variant-restricted per actions value. stepRow_interactive dropped (onSelect presence, visually identical at rest). Icon library has no minus or arrow-right: skipped minus drawn as a rounded rect, Resume button is text-only (the Button icon slot is leading, code puts the arrow trailing). |
| components/StackComponentHeaderChip | build | 255-17 | connected | matrix: locked: false/true. When not locked: hasLogo: true/false. Total effective variants: 3 (withLogo, noLogo, locked). · deps: LockedResourceTag · connect: hasLogo: figma.boolean(); locked: figma.boolean(); label: figma.string() · When locked=true the entire chip is replaced by a LockedResourceTag instance. The logo is a 14x14 img slot. Without a logo the chip renders only the mono text label at the same fixed height. |
| components/CopyButton | map-to-existing | 65-26 | connected | CONNECTED 2026-07-07: points at the existing IconButton node; variant/size enums delegated. Copy vs Check (copied) is a runtime icon swap, not a Figma variant. No new Figma build. |
| components/DebouncedInput | map-to-existing | 66-11 | connected | CONNECTED 2026-07-07: points at the existing Input node; placeholder + disabled mapped, debounce is behavior-only. No new Figma build. |
| components/LockedResourceTag | build | 254-17 | connected | matrix: width: auto/constrained(truncated). Total: 2 variants. · deps: Tooltip (existing Figma page) · connect: name: figma.string(); truncated: figma.boolean() · Always renders the same visual surface: muted/50 bg, border, lock icon (3x3), mono label. The truncated variant shows ellipsis with the full name deferred to the Tooltip. No color or size axes, single shape, two width treatments. |
| components/ProvenanceStrip | build | 416-1335 | connected | matrix: role Replay/Original, 2 total · deps: - · connect: figma.enum('role', { Replay: 'replay', Original: 'original' }) · Compact execution provenance header: Replay shows source link with stats (reuse, savings, cost); Original shows origin marker. Props sourceLabel, sourceRender, stats supplied at runtime. |
| components/TimelineAxis | build | 349-1197 | connected | BUILT 2026-07-07 (page `TimelineAxis`): gapContext plain/with-gap-tint, 640px rulers of mono/2xs ticks with 1x6 hairline stems (first tick left-anchored, last right-anchored, middle centered per the transform rules). with-gap-tint carries a status/warning 10% idle band. Connect maps both variants to the plain timeMap usage (the tint is composed by the waterfall header). figma.config.json gained a src/components/timeline/* importPaths entry so subdir connects emit the right import. |
| components/TimelineGapStripe | build | 350-1224 | connected | BUILT 2026-07-07 (page `TimelineGapStripe`): tone neutral/warning x hasDescription, each variant = host-painted band tint (muted-foreground/20 or warning/15) + the tooltip card (ColorDot header, duration row, optional description). New text style mono/2xs-semibold (JBM has no SemiBold; mapped Bold). Connect: tone -> gap.tone, hasDescription -> gap.description presence; IdleGap object literal in the example parses fine. |
| components/TimelineRow | build | 353-1249 | connected | BUILT 2026-07-07 (page `TimelineRow`): ONE 11-variant sparse set, axes isSelected x status (completed/running/failed/cached) x hierarchy (leaf/parent-collapsed/parent-expanded): 8 leaf combos + parent-collapsed(completed) + parent-expanded(running) + selected parent-expanded(completed). 880px rows: 264px sticky name cell (border-r, selected 3px primary edge + accent fill), 530px track (foreground/10) with status bars, mono/2xs duration (primary semibold when selected, warning/muted accents). 4 slot rows under the set demo badge/nameBadge/accent/nameCellClassName; refresh-ccw stands in for RefreshCw and the pencil is a drawn vector (neither in the icon library). Connect: status -> colorClass+accent, hierarchy -> hasChildren+expanded. |
| components/TimelineMarker | build | 356-67 | connected | BUILT 2026-07-07 (page `TimelineMarker`): label with-text/icon-only. Grip pill (drawn 6-dot GripVertical, brand/primary fill, primary-foreground dots + text/xs label) over a 120px dashed primary position line. Connect maps label; positionPct stays a runtime rail percentage. |
| components/TimelineMinimap | skip | - | skipped | Signed off (batch 1 chat). Returns null without a live scrollContainer ref; its entire visual state (bar positions, viewport box placement and size) is computed from runtime scroll metrics, there are no static meaningful variants to design against in Figma. |
| components/TimelineWaterfall | skip | - | skipped | Signed off (batch 1 chat). Pure behavioral composite shell that assembles TimelineAxis, TimelineRow, TimelineGapStripe, and TimelineMinimap over a live TimeMap with controlled zoom, ResizeObserver-driven column resizing, and keyboard handlers, all visual identity lives in the sub-components; no independent static surface to build. |


## Batch log

- Batch 1 (2026-07-06): brand leaves (ZenmlMarkIcon, KitaruMarkIcon, ZenmlLogo, KitaruLogo, BrandMarkTile, NavbarBrandSlot). Skip list signed off in chat.
- Batch 2 (2026-07-06): simple visuals (progress, slider, textarea, toggle-group, LockedResourceTag, StackComponentHeaderChip).
- Batch 3 (2026-07-06): switchers + identity (SwitcherPill set, SwitcherDropdownMenu, PageIdentityHeader set, WorkspaceProjectCard, 6 variant-restricted pill/band connects).
- Batch 4 (2026-07-06, reviewed + approved 2026-07-07): nav + onboarding (TabsBar, AppNavbar, OnboardingProgress, OnboardingChecklist, OnboardingWidget).
- Batch 5 (2026-07-07, FINAL): timeline (TimelineAxis, TimelineGapStripe, TimelineRow, TimelineMarker), overlays (alert-dialog, dialog, sheet), map-to-existing connects (OrgMark, Breadcrumbs, CopyButton, DebouncedInput). Parse gate closed at 114 React entries + 58 story design links, exit 0 (progress.figma.tsx InternalError is baseline noise).

**Phase 2 complete.** All 44 gap rows are connected, mapped, or signed-off skips. Library icons were converted to outline fills mid-batch-5 (Zuri, 2026-07-07); all batch-5 icon instances recolor via vector fills, and batches 1-4 were audited clean of stroke-based recolors.

**Phase 3 (post-publish Dev Mode spot-check)** is gated on the next staging -> main batch merge: publish the library, then verify a sample of connects resolve in Dev Mode against the published components.
